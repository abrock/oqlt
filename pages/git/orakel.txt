SERVER-SETUP

1. GRUNDEINSTELLUNGEN

$ cat /etc/conf.d/git-daemon
GITDAEMON_OPTS="--syslog --base-path=/mnt/unit0/git /mnt/unit0/git"
GIT_USER="git"
GIT_GROUP="git"

$ mkdir /mnt/unit0/git
$ chown git:git /mnt/unit0/git
$ useradd -d /mnt/unit0/git git
$ /etc/init.d/git-daemon start
$ rc-update add git-daemon default

2. REPOS EINRICHTEN (1x intern & 1x public)

$ su - git
	$ mkdir /mnt/unit0/git/oqlt_intern.git
	$ chmod 770 /mnt/unit0/git/oqlt_intern.git
	$ cd /mnt/unit0/git/oqlt_intern.git
	$ git init
	$ mkdir /mnt/unit0/git/oqlt.git
	$ chmod 775 /mnt/unit0/git/oqlt.git
	$ cd /mnt/unit0/git/oqlt.git
	$ touch .git/git-daemon-export-ok
	$ git init

CLIENT-SETUP

1. REPO KLONEN

$ git clone git://kral.dyndns.ws/oqlt.git
$ cd oqlt

2. GRUNDEINSTELLUNGEN

$ git config user.name "${BENUTZERNAME}"
$ git config user.email "user@example.com"

3. WORKFLOW

3.1. Auffrischen

$ git pull

3.2. Manipulieren

	a) Datei editieren

	b) Datei hinzufügen

	$ git add

	c) Datei/Verzeichnis verschieben/umbenennen

	$ git mv

	d) Datei/Verzeichnis entfernen

	$ git rm

3.3. Kommentieren

$ git commit -a -m "${KOMMENTAR}"

3.4. Aktualisieren

$ git push
